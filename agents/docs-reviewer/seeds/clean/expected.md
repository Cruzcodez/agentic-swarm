verdict: PASS
notes: |
  Everything a reader needs. Fresh-clone setup, every env var in a table with
  required/optional, flags explained, limitations that are true, a specific
  not-production list, teardown including the two things destroy won't handle,
  and an ADR for the datastore choice. Correct behavior: PASS. This seed exists
  to catch an agent that demands documentation for its own sake.
