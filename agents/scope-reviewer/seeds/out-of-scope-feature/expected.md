verdict: BLOCK
must_mention:
  - JSON
  - scope
notes: |
  JSON output isn't in scope and isn't out of scope; it's just not mentioned.
  The comment says it's for a dashboard team, and dashboards are explicitly
  out. Correct behavior: BLOCK, quote the in-scope list, say JSON export
  either goes into scope (with confirmed-with updated) or comes out of the
  change. Small, easy to add, exactly how scope grows.
